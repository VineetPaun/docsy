# Generated by `bunx convex dev` — never hand-edited, and reformatting it makes
# every regeneration a diff (CLAUDE.md trap 4).
convex/_generated/

# Lockfiles, build output, dependencies.
bun.lock
.next/
node_modules/
next-env.d.ts

# shadcn primitives are pulled in by the CLI and re-pulled on update; leaving
# them as the registry ships them keeps `shadcn add` diffs to real changes.
components/ui/
